name: CI
'on':
  push:
    branches-ignore:
      - release
  pull_request:
    branches-ignore:
      - release
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm i
      - run: npm run build
